<%= form_with(@gamecomment, url: game_game_comments_path(game_id: @gamecomment.game_id), local:true) do |f| %>


  <%= f.text_area :comment %>
  <div>
    <%= f.submit "新規登録", class: "btn btn-info active" %>
  </div>

<% end %>

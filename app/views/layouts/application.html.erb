<!DOCTYPE html>
<html>
  <head>
    <title>Boardtoy</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/30037d6a6c.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-dark">
        <div class="navbar-nav mr-auto">
          <li><%= link_to ' トップページへ',  root_path, class: "btn btn-outline-primary"%>&emsp;</li>
          <% if user_signed_in? && current_user.management_permissions ==true %>
          <li><%= link_to ' ゲーム詳細追加',  new_game_path, class: "btn btn-outline-primary"%>&emsp;</li>
          <% end %>
        </div>
        <ul class="navbar-nav">
          <li><%= link_to ' 掲示板へ',  topics_path, class: "btn btn-outline-primary"%>&emsp;</li>
          <li><%= link_to ' ジャンル選択へ',  genres_games_path, class: "btn btn-outline-primary"%>&emsp;</li>
        <% if user_signed_in? %>
          <li><%= link_to ' お気に入り商品', user_likes_path(current_user), class: "btn btn-outline-primary"%>&emsp;</li>
          <li><%= link_to ' マイページ',  user_path(current_user), class: "btn btn-outline-primary"%>&emsp;</li>
          <li><%= link_to ' ログアウト', destroy_user_session_path, method: :delete, class: "btn btn-outline-primary" %>
        <% else %>
          <li><%= link_to ' 新規登録',  new_user_registration_path, class: "btn btn-outline-primary"%>&emsp;</li>
          <li><%= link_to 'ログイン', new_user_session_path, class: "btn btn-outline-primary"%></li>
        <% end %>
    </header>
  <body>
    <div class="container body-down">
        <div class="body">
          <%= yield %>
        </div>
    </div>
  </body>
</html>
